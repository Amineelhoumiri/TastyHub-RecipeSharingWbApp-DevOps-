'use client';

export default function DarkModeScript() {
  // This component is intentionally empty
  // The dark mode script is already in the layout.js head section
  // This component exists only to satisfy the import
  return null;
}
